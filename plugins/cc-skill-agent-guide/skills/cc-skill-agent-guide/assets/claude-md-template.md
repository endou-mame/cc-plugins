# CLAUDE.md

## 概要（WHY）

{プロジェクトの目的を 1-2 文で}

## 技術スタック（WHAT）

- 言語: {TypeScript / Python / etc.}
- フレームワーク: {Express / FastAPI / etc.}
- DB: {PostgreSQL / MySQL / etc.}

## ディレクトリ構成（WHAT）

- `src/` - {説明}
- `tests/` - {説明}
- `docs/` - {説明}

## コマンド（HOW）

- `{build command}` - ビルド
- `{test command}` - テスト
- `{lint command}` - リント

## ドキュメント（Progressive Disclosure）

作業前に関連ドキュメントを読むこと:

- `docs/architecture.md` - システム構成
- `docs/api-spec.md` - API 仕様
- `docs/db-schema.md` - DB スキーマ

## {agent-name} エージェント

以下の場合、**必ず** Task ツールで `{agent-name}` を使用:

- {トリガー条件 1}（例:「{具体例 1}」「{具体例 2}」）
- {トリガー条件 2}（例:「{具体例 3}」「{具体例 4}」）
